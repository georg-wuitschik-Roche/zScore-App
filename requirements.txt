dash>=2.0.0
pandas>=1.5.0
plotly>=5.0.0
numpy>=1.20.0  # Required for array operations and statistical functions in plot_utils.py
kaleido>=0.2.0  # Required for exporting plots as images
gunicorn>=20.0.0  # Required for production deployment with WSGI server
google-cloud-storage>=2.0.0  # Optional: Only needed for cloud data loading
requests>=2.25.0  # Required for downloading data from cloud storage